services:
  conexao-chapada-api:
    container_name: conexao-chapada-api
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    restart: unless-stopped
    # ADICIONE ISSO AQUI:
    volumes:
      - /root/conexao-chapada-api:/app
      - /app/node_modules